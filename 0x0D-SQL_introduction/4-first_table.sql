-- Create a table in the current database
-- Query to create a table called first_table in the current database in your NySQL server
CREATE TABLE IF NOT EXISTS first_table (
id INT,
name VARCHAR(256));
